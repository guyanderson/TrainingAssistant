@model IEnumerable<TrainingHelper.Models.Certification>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<h4>Here's the list of all Certifications in the Fab. Choose one to Edit or Delete.</h4>
<hr />

<div class="row">
    <ul>
        @foreach (var x in Model)
        {
            <li>
                @Html.DisplayFor(modelItem => x.Name) |
                @Html.ActionLink("Edit", "Edit", new { id = x.CertificationId }) |
                @Html.ActionLink("Delete", "Delete", new { id = x.CertificationId })
            </li>
        }
    </ul>
</div>